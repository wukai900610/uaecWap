const getters = {
    skin : state => state.app.skin,
    host:state=>state.app.host,
    token:state=>state.app.token,
    element:state=>state.app.element,
    viewShare:state=>state.app.viewShare,
    code:state=>state.app.code
  }
  export default getters
